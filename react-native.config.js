module.exports = {
  assets: [], // Add your custom fonts folder if needed
  dependencies: {
    'react-native-vector-icons': {
      platforms: {
        ios: null, // Prevent automatic linking on iOS
      },
    },
  },
};
